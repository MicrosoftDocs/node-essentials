{
  "name": "testrunner",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "npm run build && node --test",
    "test:dev": "npm run build && node --watch --test test/ "
  },
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "@types/node": "^22.13.10"
  }
}
