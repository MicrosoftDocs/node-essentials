{
  "name": "testrunner",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "node --experimental-strip-types --test --experimental-test-coverage --experimental-test-module-mocks",
    "test:dev": "npm run build && node --watch --test test/ "
  },
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "@types/node": "^22.13.10"
  }
}
